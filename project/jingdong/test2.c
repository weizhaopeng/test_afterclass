#include <stdio.h>




/*
typedef struct node {
	int a, b, c;
} node;

typedef struct node_array{
	node *node_set;
	int   length;
	int	  having;
} node_array;

int main (int argc, char **argv) {
	int node_num;
	node node_temp;

	scanf("%d", &node_num);

	node 	   *set = calloc(1, sizeof(node)*node_num);
	node_array *na  = calloc(1, sizeof(node_array));
	
	na->node_set	= set;
	na->length		= node_num;
	na->having		= 0;

	for (int i = 0; i < node_num; i++) {
		scanf("%d%d%d", &(node_temp.a), &(node_temp.b), &(node_temp.c));
		if (i == 0) {
			set[i]->a = node_temp.a;
			set[i]->b = node_temp.b;
			set[i]->c = node_temp.c;
		}
		else {
			for (int j = 0; j <= node_array->having; j++) {
				if (node_temp.a 

*/
		
		
